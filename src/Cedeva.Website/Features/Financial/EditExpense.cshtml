@model Cedeva.Website.Features.Financial.ViewModels.ExpenseViewModel
@inject IStringLocalizer<SharedResources> Localizer
@using Cedeva.Website.Localization

@{
    ViewData["Title"] = Localizer["Expense.EditExpense"];
}


<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary">
                    <h5 class="mb-0 text-white">
                        <i class="fas fa-edit me-2"></i>@Localizer["Expense.EditExpense"]
                    </h5>
                </div>
                <div class="card-body">
                    <form asp-action="EditExpense" asp-route-id="@Model.Id" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="ActivityId" />
                        <input type="hidden" name="returnUrl" value="@ViewData["ReturnUrl"]" />

                        <div class="mb-3">
                            <label for="Label" class="form-label">@Localizer["Field.Label"]</label>
                            <input asp-for="Label" class="form-control" id="Label" />
                            <span asp-validation-for="Label" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label for="Description" class="form-label">@Localizer["Field.Description"]</label>
                            <textarea asp-for="Description" class="form-control" id="Description" rows="3"></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="Amount" class="form-label">@Localizer["Field.Amount"]</label>
                                <div class="input-group">
                                    <input asp-for="Amount" class="form-control" id="Amount" step="0.01" />
                                    <span class="input-group-text">€</span>
                                </div>
                                <span asp-validation-for="Amount" class="text-danger"></span>
                            </div>

                            <div class="col-md-6 mb-3">
                                <label for="ExpenseDate" class="form-label">@Localizer["Field.ExpenseDate"]</label>
                                <input asp-for="ExpenseDate" class="form-control" id="ExpenseDate" type="date" />
                                <span asp-validation-for="ExpenseDate" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="Category" class="form-label">@Localizer["Field.Category"]</label>
                            <input asp-for="Category" class="form-control" id="Category" />
                            <span asp-validation-for="Category" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label for="AssignedTo" class="form-label">@Localizer["Expense.AssignedTo"]</label>
                            <select asp-for="AssignedTo" class="form-select" id="AssignedTo" asp-items="ViewBag.AssignedToList">
                                <option value="">@Localizer["Expense.SelectAssignedTo"]</option>
                            </select>
                            <span asp-validation-for="AssignedTo" class="text-danger"></span>
                        </div>

                        <div class="mb-3" id="expenseTypeDiv">
                            <label for="ExpenseType" class="form-label">@Localizer["Field.ExpenseType"]</label>
                            <select asp-for="ExpenseType" class="form-select" id="ExpenseType">
                                <option value="">@Localizer["Expense.SelectType"]</option>
                                <option value="@Cedeva.Core.Enums.ExpenseType.Reimbursement">@Localizer["ExpenseType.Reimbursement"]</option>
                                <option value="@Cedeva.Core.Enums.ExpenseType.PersonalConsumption">@Localizer["ExpenseType.PersonalConsumption"]</option>
                            </select>
                            <small class="form-text text-muted">@Localizer["Expense.ExpenseTypeHelp"]</small>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            @{
                                var returnUrl = ViewData["ReturnUrl"]?.ToString();
                                var cancelUrl = !string.IsNullOrEmpty(returnUrl) ? returnUrl : Url.Action("Transactions");
                            }
                            <a href="@cancelUrl" class="btn btn-outline-secondary">
                                <i class="fas fa-times me-1"></i>@Localizer["Button.Cancel"]
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save me-1"></i>@Localizer["Button.Save"]
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function() {
            CedevaChoices.initAll('.form-select');
        });

        function updateExpenseTypeVisibility() {
            const value = document.getElementById('assignedToSelect').value;
            const expenseTypeDiv = document.getElementById('expenseTypeDiv');

            // Show ExpenseType only if a team member is selected (numeric value)
            if (value && !isNaN(value)) {
                expenseTypeDiv.style.display = 'block';
            } else {
                expenseTypeDiv.style.display = 'none';
            }
        }

        document.getElementById('assignedToSelect').addEventListener('change', updateExpenseTypeVisibility);

        // Run on page load
        updateExpenseTypeVisibility();
    </script>
}
