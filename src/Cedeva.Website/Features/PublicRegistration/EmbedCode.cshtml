@{
    ViewData["Title"] = "Code d'intégration iframe";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="display-6"><i class="fas fa-code me-2"></i>@ViewData["Title"]</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card shadow mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-window-maximize me-2"></i>Code d'intégration</h5>
                </div>
                <div class="card-body">
                    <p>Copiez et collez ce code HTML sur votre site web pour intégrer le formulaire d'inscription:</p>

                    <div class="position-relative">
                        <pre class="bg-light p-3 border rounded"><code id="embedCode">@ViewBag.EmbedCode</code></pre>
                        <button class="btn btn-sm btn-outline-primary position-absolute top-0 end-0 m-2" onclick="copyToClipboard()">
                            <i class="fas fa-copy me-2"></i>Copier
                        </button>
                    </div>
                </div>
            </div>

            <div class="card shadow">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0"><i class="fas fa-info-circle me-2"></i>Instructions</h5>
                </div>
                <div class="card-body">
                    <ol>
                        <li class="mb-2">Copiez le code HTML ci-dessus</li>
                        <li class="mb-2">Collez-le dans le code source de votre page web à l'endroit où vous souhaitez afficher le formulaire</li>
                        <li class="mb-2">Le formulaire s'affichera automatiquement avec vos activités disponibles</li>
                        <li class="mb-0">Les inscriptions seront automatiquement enregistrées dans votre tableau de bord Cedeva</li>
                    </ol>

                    <div class="alert alert-info mt-3 mb-0">
                        <i class="fas fa-lightbulb me-2"></i>
                        <strong>Astuce:</strong> Vous pouvez également partager directement le lien suivant:
                        <a href="@ViewBag.IframeUrl" target="_blank">@ViewBag.IframeUrl</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0"><i class="fas fa-eye me-2"></i>Aperçu</h5>
                </div>
                <div class="card-body p-0">
                    <iframe src="@ViewBag.IframeUrl" width="100%" height="600" frameborder="0" style="border: 1px solid #ddd;"></iframe>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function copyToClipboard() {
            const code = document.getElementById('embedCode').textContent;
            navigator.clipboard.writeText(code).then(() => {
                alert('Code copié dans le presse-papiers!');
            }).catch(err => {
                console.error('Erreur lors de la copie:', err);
            });
        }
    </script>
}
