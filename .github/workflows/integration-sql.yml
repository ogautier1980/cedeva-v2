name: Integration tests (PostgreSQL)

# Integration tests against a real PostgreSQL 17 via Testcontainers (Docker). Independent of
# the deploy workflow: gives a pass/fail signal but does not gate the deployment. Catches
# behaviour SQLite masks (ILIKE case-insensitivity, LINQ translation) and verifies the real
# migration chain applies to a fresh database.
on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '**/*.md'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  sql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.x'

      - name: Run PostgreSQL integration tests
        run: dotnet test tests/Cedeva.Tests.Sql -c Release
